# System Configuration Bootstrap

This is a bootstrap for my system configuration. It will install all the tools
that I use on my Mac and set up my dotfiles.

```
git clone https://github.com/sailorgeoffrey/system-config.git`
cd system-config
./bootstrap.sh
```
